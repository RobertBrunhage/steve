services:
  steve:
    build: .
    container_name: steve
    restart: unless-stopped
    stdin_open: true
    tty: true
    ports:
      - "3000:3000"
    volumes:
      - steve-data:/data
      - steve-vault:/vault
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - STEVE_HOSTNAME=${STEVE_HOSTNAME:-localhost}
      - STEVE_OPENCODE_IMAGE=steve-opencode
    cap_drop:
      - NET_RAW
      - NET_ADMIN
    networks: [steve-net]

volumes:
  steve-data:
  steve-vault:

networks:
  steve-net:
    driver: bridge
